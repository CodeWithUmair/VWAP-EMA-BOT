# Handoff

The working handoff lives in **[`docs/handoff/README.md`](docs/handoff/README.md)** —
start there. This file is just a signpost at the repo root.

- `docs/handoff/README.md` — current state, how to run, index, open questions
- `docs/handoff/decisions.md` — why the bot is built the way it is (reverse-engineered)
- `docs/handoff/MULTI-ACCOUNT.md` — running this bot on several Exness accounts at once
- `docs/handoff/sessions/` — one file per working session

This tree was added 2026-09-01 by an outside session and is the **only** thing added to
this fork so far — no bot code (`trading_bot/`, `src/`, `server.ts`, root) was changed.
